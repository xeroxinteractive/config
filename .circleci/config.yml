version: 2

defaults: &defaults
  working_directory: ~/repo
  docker:
    - image: circleci/node:lts
  resource_class: small

jobs:
  install_lint_test:
    <<: *defaults
    steps:
      - checkout
      - restore_cache:
          keys:
            - node-10-cache-{{ checksum "./yarn.lock" }}
      - run: yarn install
      - run: yarn lint
      - run: yarn test
      - save_cache:
          key: node-10-cache-{{ checksum "./yarn.lock" }}
          paths:
            - ~/.yarn-cache
            - ./node_modules
      - persist_to_workspace:
          root: ~/repo
          paths:
            - ./

  deploy:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - add_ssh_keys:
          fingerprints:
            - '94:aa:2d:64:14:93:c8:2f:cb:98:b5:4f:ec:96:80:86'
      - run:
          name: Configure git
          command: |
            ssh-keyscan -H github.com >> ~/.ssh/known_hosts
            git config --global user.email "xeroxinteractive-circleci@xerox.com"
            git config --global user.name "CircleCI"
      - run:
          name: Deploy package to npm
          command: yarn deploy

  browserslist-stats:
    <<: *defaults
    steps:
      - checkout
      - run:
          name: Configure git
          command: |
            ssh-keyscan -H github.com >> ~/.ssh/known_hosts
            git config --global user.email "jenkinsbuilder@documentsmart.com"
            git config --global user.name "xeroxinteractive-builder"
      - run:
          name: Switch to new branch
          command: |
            CRON_BRANCH_NAME="cron/stats/$(date +%d-%m-%Y)"
            git checkout -b $CRON_BRANCH_NAME
      - restore_cache:
          keys:
            - node-10-cache-{{ checksum "./yarn.lock" }}
      - run: yarn install
      - run:
          name: Create new stats file
          command: |
            BAA_PRIVATE_KEY="$(echo $PRIVATE_KEY | base64 --decode)" yarn workspace @xerox/browserslist-config run update:stats
            git commit -a -m 'chore(browserslist-config): update stats'
      - run:
          name: Push branch and create PR
          command: |
            cd ~
            wget https://github.com/github/hub/releases/download/v2.11.2/hub-linux-amd64-2.11.2.tgz
            tar xvfz hub-linux-amd64-2.11.2.tgz
            cd ~/repo
            CRON_BRANCH_NAME="cron/stats/$(date +%d-%m-%Y)"
            git push --force --set-upstream origin $CRON_BRANCH_NAME
            ~/hub-linux-amd64-2.11.2/bin/hub pull-request --file .circleci/stats-cron-pr.md --base master

workflows:
  version: 2
  install_lint_test_deploy:
    jobs:
      - install_lint_test
      - deploy:
          requires:
            - install_lint_test
          filters:
            branches:
              only:
                - master
  browserslist-stats-cron:
    triggers:
      - schedule:
          cron: '50 15 2 * *'
          filters:
            branches:
              only:
                - master
    jobs:
      - browserslist-stats
